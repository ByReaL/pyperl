language: python
python:
  - "2.7"
  - "3.5"
dist: trusty
sudo: required
before_install:
  - sudo apt-get -qq update
  - sudo apt-get -y perl
  - sudo cd Python-Object
  - sudo perl Makefile.PL
  - sudo make install
install:
  - pip install
script:
  - test.py